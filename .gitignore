# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.vs/
.idea/

# Stray screenshots dropped at repo root — canonical ones live under specs/**/screenshots/
/*.png

# Sibling code repositories cloned inside this governance repo — independent repositories,
# never submodules (docs/sdlc/repository-strategy.md)
/fitforge-api
/fitforge-web

# Local environment files
.env
.env*.local

# Add your stack's build-output ignores in the code repositories themselves,
# not here — this repository holds governance and specs only.
